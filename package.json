{
  "name": "loyalty-pro",
  "version": "0.1.0",
  "private": true,
  "description": "Loyalty Pro (ولاء برو) — loyalty journey, catalogue, storefront and orders — monorepo root",
  "packageManager": "pnpm@10.34.4",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "pnpm verify:identity && turbo run test",
    "verify:identity": "node packaging/scripts/verify-identity.mjs",
    "dev": "turbo run dev",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,yaml,yml}\"",
    "package:build": "pnpm version:check && pnpm --filter @loyalty-pro/api db:template:check && pnpm --filter @loyalty-pro/api db:template && pnpm --filter @loyalty-pro/station build && pnpm --filter @loyalty-pro/license-native build && pnpm --filter @loyalty-pro/packaging service:build && pnpm --filter @loyalty-pro/packaging stage",
    "db:template": "pnpm --filter @loyalty-pro/api db:template",
    "db:template:check": "pnpm --filter @loyalty-pro/api db:template:check",
    "version:check": "node packaging/scripts/version.mjs",
    "version:write": "node packaging/scripts/version.mjs --write",
    "package:verify": "pnpm --filter @loyalty-pro/packaging verify && pnpm --filter @loyalty-pro/packaging verify:service",
    "package:installer": "node packaging/scripts/verify-license-key.mjs && node packaging/scripts/verify-signing.mjs --pre &&pnpm --filter @loyalty-pro/manager-desktop tauri build && node packaging/scripts/verify-signing.mjs --post",
    "verify:signing": "node packaging/scripts/verify-signing.mjs --pre",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:wait": "docker compose exec -T postgres pg_isready -U walaa -d walaa",
    "db:migrate": "pnpm --filter @loyalty-pro/api db:migrate",
    "db:generate": "pnpm --filter @loyalty-pro/api db:generate",
    "db:seed": "pnpm --filter @loyalty-pro/api db:seed",
    "db:reset": "pnpm --filter @loyalty-pro/api db:reset",
    "db:studio": "pnpm --filter @loyalty-pro/api db:studio",
    "db:reset-ceremony": "pnpm --filter @loyalty-pro/api db:reset-ceremony"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "eslint": "^9.18.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "argon2",
      "esbuild",
      "prisma",
      "sharp",
      "unrs-resolver"
    ]
  }
}
